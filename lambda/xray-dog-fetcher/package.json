{
  "name": "xray-dog-fetcher",
  "version": "1.0.0",
  "description": "X-Ray POC Lambda - fetches dog image and invokes s3-writer",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@aws-sdk/client-lambda": "^3.600.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
