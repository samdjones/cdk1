# CDK1

AWS CDK project with an Express app running on ECS Fargate and a Lambda function.

# Testing

The endpoint url with /health and /multiply endpoints is:
http://cdk1st-servi-bg5okwtgavfl-205542098.us-east-1.elb.amazonaws.com/health

## Prerequisites

- Node.js 22+
- Python 3.12+
- AWS CLI configured with credentials
- AWS CDK CLI (`npm install -g aws-cdk`)
- Docker (for building container images)
